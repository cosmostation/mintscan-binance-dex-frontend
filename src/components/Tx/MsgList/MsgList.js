import * as React from "react";
import styles from "./MsgList.scss";
import cn from "classnames/bind";

const cx = cn.bind("styles");

export default function(props) {
	return (
		<div className={cx("MsgList-wrapper")}>
			asdf
			<div>asdf</div>
		</div>
	);
}
